n1,p1,f1 : INT ;

d_s1,e_s1 : INT ;

A: TYPE = ARRAY INT OF REAL;
arr: A;
arr_array1: INT = 5;
arr_array2: INT = 10;
elem: REAL = arr[arr_array1][arr_array2] ;

i : INT ;

ASSERT i=0 ;

ASSERT arr[0][0]=0 ;

ASSERT FORALL ( x : INT) : i< n1 =>
arr[0][1] = arr[0][1] + 1 AND
i=i+1 ;

n,p,f : INT ;

d_t,e_t : INT ;

ASSERT d_t = (n + p) * f + 1 ;

ASSERT e_t = (n - p) + (f +  1) ;

CHECKSAT NOT f = f1 ;
